@model App.ViewModels.EditUserViewModel

@{
    ViewData["Title"] = "Edit Driver";
}

<h1>Edit Driver</h1>

<h4>Race</h4>
<hr />
<p>
    <a asp-action="Create">Create a Car</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Id
            </th>
            <th>
                Car Name
            </th>
            <th>
                Date
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.Driver.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.Driver.LastName)
            </td>
             <td>
                @Html.DisplayFor(modelItem => Model.Driver.Email)
            </td>
        </tr>
    </tbody>
</table>
@* <div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CarId" class="control-label"></label>
                <input asp-for="CarId" class="form-control" />
                <span asp-validation-for="CarId" class="error-label"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div> *@

<div>
    <a asp-action="Index">Back to List</a>
</div>

